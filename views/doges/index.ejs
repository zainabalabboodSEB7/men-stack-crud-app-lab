<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Dogs Home Page</title>
</head>
<body>
    <div class="index-header-div">
        <h1>All Dogs</h1>
        <a href="/doges/new" class="add-dogs">Add Dogs</a>
    </div>
		<ul>
			<% allDogs.forEach(dogs => { %>
			<li>
				<h2>
					<a href="/doges/<%= dogs._id %>">
						<%= dogs.name %>
					</a>
                    <% if (dogs.isVaccinated) { %>
                        ✅ 
                    <% } %>
				</h2>
				<p><strong>Age:</strong> <%= dogs.age %></p>
				<p><strong>Gender:</strong> <%= dogs.gender %></p>
				<p><strong>Description:</strong> <%= dogs.description %></p>
				<p><strong>Color:</strong> <%= dogs.color %></p>
				<p><strong>Price:</strong> <%= dogs.price %></p>
				<% if (dogs.website) { %>
				<p>
					<strong>Website:</strong>
					<a href="<%= dogs.website %>" target="_blank">
                        <%= dogs.website %>
                    </a>
				</p>
				<% } %>
				<hr />
			</li>
			<% }) %>
		</ul>
</body>
</html>